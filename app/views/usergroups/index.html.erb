
<% @usergroups.each do |group| %>
  <%= link_to group.name, usergroup_path(group) %>
<% end %>


<div class="back-link top-padded">
  <%= link_to 'Add New Group', new_usergroup_path, method: :get %>
</div>
